#! python

import sys

# nothing is selected = everything is selected
# with selected objects

# foreach object 
	# calculate the bounding box
	# based on the size of the bounding box
	# set the viewdistance to a size
	# if 

#Set the view distance
def setViewDist(viewDist, objName):
	#code...
	general.set_entity_param(objName, 'ViewDistRatio', viewDist)

